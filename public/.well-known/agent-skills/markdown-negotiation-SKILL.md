# Markdown Negotiation Skill

When a client sends Accept: text/markdown, return a markdown representation of the page.

Requirements:

- Content-Type: text/markdown
- Vary: Accept
- Keep HTML as default for browsers
